# vi: set ft=changelog :
Sebastien Badia - Sebian <se@sebian.fr>
  * tests:
    - nova-manage bd sync
  * features:
    - gestion ip virtuelles (g5k) => working in nova, but un-reserved on g5k…


  * Ecole: afficher les outputs puppet (pour meubler)
  * vm ip pub: ip addr show
  * ec2_api: unset pour la variable
